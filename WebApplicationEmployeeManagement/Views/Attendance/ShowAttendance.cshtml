@model List<DAL.Models.Attendance>
@{
    ViewBag.Title = "ShowAttendance";

    //@foreach (var item in Model)
    //{
    //    //DateTime timeAttendance = new DateTime(item.TimeAttendance.Ticks);
    //    //DateTime timeLeave = new DateTime(item.TimeLeave.Ticks);
    //    //TimeSpan total = timeAttendance - timeLeave;


    //    var hours = (item.TimeAttendance - item.TimeLeave ).TotalHours;
    //}

}

<h2>Show Attendance @ViewBag.date.ToString("yyyy-MM-dd") </h2>

<div>
    <form asp-controller="Attendance" asp-action="ShowAttendance" method="post">
        <input type="date" name="date" />
        <button type="submit">SEARCH </button>
    </form>
</div>

<table class="table table-striped table-hover">
    <thead>
        <tr>
            <th>Time Attendance</th>
            <th>Time Leave</th>
            <th>Employee Name</th>
            <th>Hours Job</th>
            <th>Shift</th>
        </tr>
    </thead>

    <tbody>
        @foreach (var item in Model)
        {
            //var different = item.TimeLeave - item.TimeAttendance;

            <tr>
                <td>@item.TimeAttendance.ToString("yyyy-MM-dd hh:mm:ss tt")</td>
                <td>@item.TimeLeave.ToString("yyyy-MM-dd hh:mm:ss tt")</td>
                <td>@item.Employees.Name</td>

                @if (item.Duration.Hours == 8)
                {
                    <td class="text-success">@item.Duration</td>
                }
                else if (item.Duration.Hours < 8)
                {
                    <td class="text-danger">@item.Duration</td>
                }
                else if (item.Duration.Hours > 8)
                {
                    <td class="text-warning">@item.Duration</td>
                }

                <td>@item.Shift.ShiftName</td>
            </tr>
        }
    </tbody>
</table>
